### 已完成事项
- 确认可采用飞牛自建 Gitea 单独存放 LoginServer 仓库，与 Unity 游戏仓分离

### 关键决策
- 推荐：游戏客户端继续 GitHub；登录/存档服务独立为飞牛 Gitea 的 `LoginServer` 仓库
- 云端 Agent 需用户提供 Gitea 地址与访问权限后，才能直接推送到 NAS 上的仓库

### 未完成待办
- 用户在飞牛 Gitea 创建 `LoginServer` 仓库并告知地址/权限
- 将现有 `server/` 初始化推送到该仓库（或提供迁移步骤）
- 飞牛 Docker Compose 改为从该仓库拉取部署
