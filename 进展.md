### 已完成事项
- 说明 #5/#6 分支关系，并按你的要求改为长期固定开发分支
- 新建长期分支 `cursor/mh-idle-dev-17dc`（基于已合入 #6 的 main）
- 约定：日常只在该分支提交推送；**只有你明确说「开 PR」时才创建 PR**
- 落地 Python FastAPI 最小存档服：`server/`（登录/拉档/推档 + SQLite）
- 增加 Unity `CloudSaveClient` 雏形

### 关键决策
- 服务器技术栈：Python + FastAPI + SQLite
- 账号服与主游戏隔离；微信仍用同一小游戏 AppID
- 开发期支持 `guest` 登录，正式再接 `code2session`

### 未完成待办
- 把本地 `SaveSystem` 真正接到云存档上下行
- 服务器部署到你的公网机器（Nginx + HTTPS）
- 微信正式登录配置 AppId/Secret
- 主游戏按钮进入本玩法（同小游戏分包/场景）
