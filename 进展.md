### 已完成事项
- 为飞牛 NAS 准备 Docker 部署：`Dockerfile`、`docker-compose.yml`、`飞牛NAS部署.md`
- 数据库路径支持环境变量 `MH_DB_PATH`，便于挂载持久化卷
- 说明：无法从云端直接登录用户家中飞牛，需用户在 NAS 上执行 compose

### 关键决策
- 飞牛优先用 Docker Compose 部署，降低 Python 环境配置成本
- 继续固定分支 `cursor/mh-idle-dev-17dc`，未自动开 PR

### 未完成待办
- 用户在飞牛执行 `docker compose up -d --build` 并反馈 IP/报错
- 外网 HTTPS 域名（微信正式环境需要）
- Unity 云存档真正对接 `CloudSaveClient`
