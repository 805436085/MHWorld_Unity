### 已完成事项
- 挂机原型 + 竖屏 UI + 道具图标/数值平衡在 `cursor/wechat-iphone12-ui-7a73`
- 新增 `IdleGameEntry.Enter/Exit`；Bootstrap 可用开关关闭自动启动
- 新增 `接入说明.md`（同 Unity 版本 → 并入主工程当新入口）

### 关键决策
- 接入已发布小游戏：**并进现网 Unity 工程**，大厅调 `IdleGameEntry`，用现网工程打微信包（同一 AppID）
- 并入后必须把 `IdleGameBootstrap.AutoLaunchInAnyScene = false`
- 不做双工程双 WASM 硬拼

### 未完成待办
- 用户在现网工程执行拷贝与大厅按钮对接
- 挂机 UI 内补「返回大厅」按钮（调 Exit）
- 存档与现网账号体系打通
- 微信转换导出与包体裁剪
