### 已完成事项
- Gitea `spoon/LoginServer` 已改为公开，可匿名克隆
- 已用访问令牌成功推送首版：`e390100`（FastAPI 存档服务、Docker Compose、飞牛部署说明）
- 远程文件已确认：`main.py`、`Dockerfile`、`docker-compose.yml`、`requirements.txt`、`README.md`、`.env.example`、`飞牛NAS部署.md`

### 关键决策
- 服务端独立仓：`http://spoon.beer:8418/spoon/LoginServer.git`
- Token 仅用于推送，不写入任何仓库；后续部署以该仓为准
- Unity 仓 `server/` 可继续作本地开发对照

### 未完成待办
- 飞牛 NAS 上 `docker compose up -d --build` 部署 LoginServer
- Unity 云存档对接（`CloudSaveClient` ↔ `/api/login` `/api/save`）
- 微信 `code2session`（同一小游戏 AppID）
- 正式环境 HTTPS 域名（微信生产需要）
