### 已完成事项
- 实现道具/陷阱流与 10 格出征背包（购买/制造/装配/战斗自动使用）
- 实现防具技能 Build（点数累计 + 多流派套装）
- 落地 v2：三圈熟练度、双轨狩猎、四页主 UI
- 已与最新 `origin/main`（含 PR #4）合并并解决冲突
- 明确飞牛 NAS LoginServer Docker 选型：`python:3.13.15-slim`，SQLite 无需独立镜像
- 用户侧容器环境自检通过（Python 3.13.15 / SQLite 3.46.1）
- 新增最小 LoginServer：`LoginServer/`（FastAPI + SQLite + JWT + Dockerfile），本地冒烟通过
- 确认可在现成 python 容器内 apt+git clone+pip 直接跑服务
- 澄清：当前 LoginServer 仅单游戏登录原型；多游戏登录/存档需扩展 game_id 与存档表

### 关键决策
- LoginServer 密码哈希用标准库 PBKDF2；JWT 用 PyJWT；数据目录默认 `/app/data`
- 飞牛现阶段优先：在现有 `python:3.13.15-slim` 容器终端内 clone 分支代码并 uvicorn 启动
- 账号可做成跨游戏统一账号；存档必须按 `game_id + user_id` 隔离，不能共用一张糊成一锅的存档

### 未完成待办
- 珠子孔位混搭
- 道具专用图标
- 数值平衡（陷阱 CD、炸弹伤害）
- 本地 Unity 实机验证
- 飞牛容器内启动 LoginServer 并验证 `/health` `/register` `/login`
- 飞牛存储卷挂载（正式存账号前）
- Unity 客户端对接登录 API
- （可选）扩展为多游戏：games 表、统一账号、按游戏隔离的存档 API
