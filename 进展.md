### 已完成事项
- 挂机模块入口 API（IdleGameEntry）与接入说明已备好
- 已澄清：双独立 Unity + 同一 AppID 跳转不可行

### 关键决策
- **同一 AppID**：一个小游戏只有一份现网包；两个 Unity 工程各自转换后用同一 AppID 上传会互相覆盖，不能「同 App 内跳转」
- 官方「多包融合」是同游戏多运行时变体（如单/多线程），不是两个不同玩法互跳
- **真要独立两工程**：只能 **两个 AppID** + `wx.navigateToMiniProgram`（会弹确认，且是跳出到另一个小游戏）
- **仍要加进已发布那个**：继续推荐并进现网 Unity 工程当新入口

### 未完成待办
- 用户选定方案：并工程（推荐）或双 AppID 跳转
- 若并工程：现网大厅接 Enter/Exit 并出包
- 存档与账号打通
