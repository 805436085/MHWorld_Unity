### 已完成事项
- 用户已将 Gitea 仓库改为公开：`spoon/LoginServer`（`private: false`）
- 已确认可匿名克隆：`git ls-remote` / `git clone` 成功；远程仅有 Initial commit（Python `.gitignore` + 空 README）
- 已在本地基于远程历史整理好完整首版提交（`e390100`）：`main.py`、Docker Compose、`飞牛NAS部署.md`、README 等
- 仍无法从 Cloud Agent 推送：写操作需要 Token（`token is required` / `could not read Username`）

### 关键决策
- LoginServer 独立仓：`http://spoon.beer:8418/spoon/LoginServer.git`（现已公开可读）
- 服务端代码以该仓为准；Unity 仓 `server/` 可先作开发镜像，部署改拉 Gitea
- Cloud Agent 无 Gitea 写权限时，由用户用 Token 推送或在 NAS 上 push

### 未完成待办
- 用户创建 Gitea Access Token（仓库写权限）并推送本地已准备好的提交，或把 Token 提供给 Agent 代推
- 推送成功后：飞牛 Docker 从该仓部署；Unity 云存档对接；微信 code2session
