### 已完成事项
- Gitea `spoon/LoginServer` 首版已推送
- 用户飞牛 Compose 项目路径定为：`/vol1/1000/LoginServer`
- 已说明：该路径需先放入仓库文件（clone 或上传），再 `docker compose up -d --build`

### 关键决策
- 飞牛部署根目录：`/vol1/1000/LoginServer`
- 可用 SSH clone，或文件管理上传；也可用 Docker Compose 网页启动

### 未完成待办
- 确认目录内有 `main.py`/`Dockerfile` 并完成构建启动
- 健康检查 `http://NAS_IP:8000/health`
- Unity 云存档对接；微信 code2session；生产 HTTPS
