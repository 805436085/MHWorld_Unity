# 接入已发布微信小游戏（同 Unity 版本 · 新入口）

适用情况：现网小游戏也是 **Unity 2022.3 + 微信转换**，要把本挂机玩法当成 **大厅里的新入口**，而不是替换主玩法。

## 结论（怎么做）

不要单独再导一个小游戏 App。  
把本仓库的挂机模块 **并进现网那个 Unity 工程**，大厅加按钮调用入口，再 **用现网工程打一次微信包** 上传同一 AppID。

```
现网大厅 Scene
   └─ 按钮「挂机狩猎」 → IdleGameEntry.Enter()
   └─ 挂机内「返回」   → IdleGameEntry.Exit()  （或你们自己的返回按钮里调）
```

## 步骤

### 1. 拷贝模块进主工程

从本仓库拷到现网工程（保持命名空间 `MHIdle`）：

- `Assets/Scripts/Idle/`（整目录）
- `Assets/Art/Icons/` + `Assets/Resources/Icons/`（道具/装备图标）
- 不必拷 `SampleScene`（可选用）

注意脚本 GUID：建议 **整夹拷贝含 .meta**，避免引用丢失。

### 2. 关掉自动启动

打开 `IdleGameBootstrap.cs`，把：

```csharp
public const bool AutoLaunchInAnyScene = true;
```

改成 `false`。  
否则一进现网任意场景就会弹出挂机 UI，盖住主玩法。

### 3. 大厅加入口

在现网大厅/菜单按钮里：

```csharp
using MHIdle;

public void OnClickIdleHunt()
{
    IdleGameEntry.Enter();
}

public void OnClickBackToLobby()
{
    IdleGameEntry.Exit();
}
```

挂机 UI 目前是全屏 OnGUI；返回大厅请务必 `Exit()`，否则会继续占屏并 DontDestroyOnLoad。

### 4. 用现网工程出微信包

1. 主工程已装微信转换 SDK（与现网同一套）
2. 填 **同一个已发布 AppID**、CDN、竖屏
3. 转换导出 → 微信开发者工具打开 → 体验版 → 正式版

包体：挂机资源尽量跟现网一样走 CDN / 分包，避免撑爆首包。

### 5. 上线前建议对齐

| 项 | 现状 | 建议 |
|----|------|------|
| 存档 | `PlayerPrefs`（`mh_idle_save_v2`） | 与现网账号/openid 绑定，避免换机丢档 |
| 登录 | 无 | 进挂机前用现网登录态 |
| UI | 独立全屏 | 后续可加「返回大厅」按钮调 `Exit()` |
| 命名空间 | `MHIdle` | 与主工程冲突时再改名 |

## 不要走的弯路

- 再注册一个新小游戏 AppID（你要的是「加进已发布」）
- 两个 Unity 工程各自转换后硬拼进一个小游戏（双份 WASM，基本不可行）
- 不改 `AutoLaunchInAnyScene` 就合并（会抢现网启动）

## 本仓库独立试玩

保持 `AutoLaunchInAnyScene = true`，Unity Play 即可。
